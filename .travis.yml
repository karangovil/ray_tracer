language: cpp
compiler:
  - gcc
  - clang
addons:
  apt:
    sources:
    - ubuntu-toolchain-r-test
    - llvm-toolchain-precise-4.0
    packages:
    - clang-9.0
install:
- "[ $CXX = clang++ ] && export CXX=clang++-9.0 || true"
script:
  - make
